<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="crm_so.list">
      <field name="name">crm_so list</field>
      <field name="model">crm_so.crm_so</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="crm_so.action_window">
      <field name="name">crm_so window</field>
      <field name="res_model">crm_so.crm_so</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="crm_so.action_server">
      <field name="name">crm_so server</field>
      <field name="model_id" ref="model_crm_so_crm_so"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="crm_so" id="crm_so.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="crm_so.menu_1" parent="crm_so.menu_root"/>
    <menuitem name="Menu 2" id="crm_so.menu_2" parent="crm_so.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="crm_so.menu_1_list" parent="crm_so.menu_1"
              action="crm_so.action_window"/>
    <menuitem name="Server to list" id="crm_so" parent="crm_so.menu_2"
              action="crm_so.action_server"/>
-->
  </data>
</odoo>